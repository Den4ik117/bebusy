<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <% if (process.env.NODE_ENV === 'production') { %>
        <link rel="stylesheet" href="/<%= manifest['resources/js/app.css'].file %>">
        <script type="module" src="/<%= manifest['resources/js/app.js'].file %>"></script>
    <% } else { %>
        <script type="module" src="http://localhost:5173/@vite/client"></script>
        <script type="module" src="http://localhost:5173/resources/js/app.js"></script>
        <script type="module" src="http://localhost:5173/resources/css/app.css"></script>
    <% } %>
    <title>Document!!</title>
</head>
<body class="antialiased bg-[#181818] text-white">
<div class="max-w-md w-full mx-auto h-[100dvh] flex flex-col justify-center items-center">
    <a
        class="text-indigo-400 px-4 py-2 rounded uppercase text-sm font-medium hover:bg-indigo-100 hover:bg-opacity-10"
        href="/oauth/redirect"
    >Войти через HeadHunter</a>

<!--    @env('local')-->
<!--    <div class="rounded-md border-2 border-dashed border-red-500 p-4 mt-4 max-w-md w-full">-->
<!--        <div class="flex flex-col gap-4">-->
<!--            <span class="uppercase text-red-500 text-xs font-semibold text-center">Тестовый контур</span>-->
<!--            @php ($users = \App\Models\User::all())-->
<!--            <ul class="flex flex-col gap-2">-->
<!--                @foreach($users as $user)-->
<!--                <li>-->
<!--                    <a-->
<!--                            class="block bg-red-500 hover:bg-red-600 hover:bg-opacity-40 bg-opacity-40 py-2 px-2 rounded-md text-sm"-->
<!--                            href="{{ route('login', ['user_id' => $user->id]) }}"-->
<!--                    >[{{ $user->id }}] {{ $user->full_name }}</a>-->
<!--                </li>-->
<!--                @endforeach-->
<!--            </ul>-->
<!--        </div>-->
<!--    </div>-->
<!--    @endenv-->
</div>
</body>
</html>
